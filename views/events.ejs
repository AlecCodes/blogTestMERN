<!DOCTYPE html>
<html lang="en">
    <body>
        <main>
            <h1>EVENTS</h1>
            <% for (event of events) { %>
                <div>
                    <h2>
                        <%= event.name  %>
                    </h2>
                </div>
            <% } %>
        </main>
    </body>

</html>