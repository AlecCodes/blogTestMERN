<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head.ejs") %>

    <body>
        <main>
            <%- include('../partials/navbar.ejs') %>
            <h1>EVENTS</h1>
            <% for (event of events) { %>
                <div>
                    <h2>
                        <%= event.name  %>
                    </h2>
                </div>
            <% } %>
        </main>
    </body>

</html>